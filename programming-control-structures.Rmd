# Control Structures {#programming-control-structures}

## Logic

* `TRUE` and `FALSE`
* `&` and `&&`
* `|` and `||`

## `if` and `else`

## Extensions

## `ifelse()`, `dplyr::if_else()`

### `data.table::fifelse()`

## Pattern Matching

### `dplyr::case_when()`

### `data.table::fcase()`